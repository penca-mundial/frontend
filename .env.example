VITE_API_URL=http://localhost:3000/api/v1

# Cloudinary (unsigned avatar upload — SCRUM-199). Baked at build time, so they
# must be set in the Vercel project for the profile photo upload to work.
VITE_CLOUDINARY_CLOUD_NAME=
VITE_CLOUDINARY_UPLOAD_PRESET=
